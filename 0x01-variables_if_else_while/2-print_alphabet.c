#include <stdio.h>

/**
 * main - prints alphabets in lower case
 * newline
 * returns 0
 */
int main(void) {
    char letter;
    for ( letter = "a"; i < "z"; letter++)
    {
        putchar(letter);
    }
    putchar("\n");
    return (0);
}
